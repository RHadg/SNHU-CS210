#pragma once

#include "Investment.h"

//calculate the user data entered
class Calculations {
public:
	void dataWithoutDeposits(Investment user_userInvestment);
	void dataWithDeposits(Investment user_userInvestment);
};